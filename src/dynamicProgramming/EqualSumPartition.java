package com.company.dynamicProgramming;

/**
 * Return true if the given array can be divided into 2 sub-arrays whose sum is equal
 *
 * Equal sum partition problem can be derived to subset sum problem.
 * If the array can be divided into 2 equal sub-arrays then it must follow these conditions:
 *      1. Sum of array elements is even
 *      2. Say x = (Sum of array elements)/2. SubSet sum for number x is true for main array
 */
public class EqualSumPartition {
    final static int [] ARRAY_NUMS = {1,2};
    public static void mainFn() {
    }
}
